import BigNumber from 'bignumber.js';
import { TokenWrapper } from '../contract_wrappers/token_wrapper';
import { BlockParamLiteral, TradeSide, TransferType } from '../types';
export declare class ExchangeTransferSimulator {
    private store;
    private UNLIMITED_ALLOWANCE_IN_BASE_UNITS;
    private static throwValidationError(failureReason, tradeSide, transferType);
    constructor(token: TokenWrapper, defaultBlock: BlockParamLiteral);
    /**
     * Simulates transferFrom call performed by a proxy
     * @param  tokenAddress      Address of the token to be transferred
     * @param  from              Owner of the transferred tokens
     * @param  to                Recipient of the transferred tokens
     * @param  amountInBaseUnits The amount of tokens being transferred
     * @param  tradeSide         Is Maker/Taker transferring
     * @param  transferType      Is it a fee payment or a value transfer
     */
    transferFromAsync(tokenAddress: string, from: string, to: string, amountInBaseUnits: BigNumber, tradeSide: TradeSide, transferType: TransferType): Promise<void>;
    private decreaseProxyAllowanceAsync(tokenAddress, userAddress, amountInBaseUnits);
    private increaseBalanceAsync(tokenAddress, userAddress, amountInBaseUnits);
    private decreaseBalanceAsync(tokenAddress, userAddress, amountInBaseUnits);
}
