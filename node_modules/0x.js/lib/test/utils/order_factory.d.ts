import BigNumber from 'bignumber.js';
import { SignedOrder, ZeroEx } from '../../src';
export declare const orderFactory: {
    createSignedOrderAsync(zeroEx: ZeroEx, maker: string, taker: string, makerFee: BigNumber, takerFee: BigNumber, makerTokenAmount: BigNumber, makerTokenAddress: string, takerTokenAmount: BigNumber, takerTokenAddress: string, exchangeContractAddress: string, feeRecipient: string, expirationUnixTimestampSecIfExists?: BigNumber | undefined): Promise<SignedOrder>;
};
