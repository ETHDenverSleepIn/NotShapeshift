import { BigNumber } from 'bignumber.js';
import { Web3EnabledService } from './web3-enabled-service';
export interface IFillOrderParams {
    nodeUrl: string;
    orderHash: string;
    /**
     * The fill amount denominated in the base token
     */
    takerAmountInWei: BigNumber;
    account: string;
}
export declare class FillOrder extends Web3EnabledService<string> {
    private readonly params;
    constructor(params: IFillOrderParams);
    protected run(): Promise<string>;
}
