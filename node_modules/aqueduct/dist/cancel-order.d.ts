import { Web3EnabledService } from './web3-enabled-service';
export interface ICancelOrderParams {
    /**
     * Ethereum node url
     */
    nodeUrl: string;
    /**
     * Order Hash
     */
    orderHash: string;
}
/**
 * Cancel an order by orderHash; returns txHash if successful
 */
export declare class CancelOrder extends Web3EnabledService<string> {
    private readonly params;
    constructor(params: ICancelOrderParams);
    protected run(): Promise<string>;
}
